    allocate( XXFVFGAS(1) ); XXFVFGAS =0.0
    allocate( XXFVFOIL(1) ); XXFVFOIL =0.0
    allocate( XXFVFWAT(1) ); XXFVFWAT =0.0
    allocate(  AMATRIX(1) ); AMATRIX  =0.0
    allocate(    DBGPO(1) ); DBGPO    =0.0
    allocate(    DBOPO(1) ); DBOPO    =0.0
    allocate(    DBWPO(1) ); DBWPO    =0.0
    allocate(   DLYTIM(1) ); DLYTIM   =0.0
    allocate( DOUBHEAD(1) ); DOUBHEAD =0.0
    allocate(    DRSSP(1) ); DRSSP    =0.0
    allocate( FIPFAMNA(1) ); FIPFAMNA ='AAAAAAAAAA'
    allocate(   FIPGAS(1) ); FIPGAS   =0.0
    allocate(   FIPOIL(1) ); FIPOIL   =0.0
    allocate(   FIPWAT(1) ); FIPWAT   =0.0
    allocate(  GAS_DEN(1) ); GAS_DEN  =0.0
    allocate(    GASKR(1) ); GASKR    =0.0
    allocate( GASQUIES(1) ); GASQUIES =0.0
    allocate( GAS_VISC(1) ); GAS_VISC =0.0
    allocate(   HIDDEN(1) ); HIDDEN   ='AAAAAAAAAA'
    allocate(     ICON(1) ); ICON     =1  
    allocate(     IGRP(1) ); IGRP     =1  
    allocate( INTEHEAD(1) ); INTEHEAD =1  
    allocate(     IWEL(1) ); IWEL     =1  
    allocate(     IWLS(1) ); IWLS     =1  
    allocate( LOGIHEAD(1) ); LOGIHEAD =.false.
    allocate(  OIL_DEN(1) ); OIL_DEN  =0.0
    allocate(    OILKR(1) ); OILKR    =0.0
    allocate( OIL_VISC(1) ); OIL_VISC =0.0
    allocate( PRESSURE(1) ); PRESSURE =0.0
    allocate(  REGDIMS(1) ); REGDIMS  =1  
    allocate(   REGRPT(1) ); REGRPT   =0.0
    allocate(       RS(1) ); RS       =0.0
    allocate(       RV(1) ); RV       =0.0
    allocate(     SCON(1) ); SCON     =0.0
    allocate(  SFIPGAS(1) ); SFIPGAS  =0.0
    allocate(  SFIPOIL(1) ); SFIPOIL  =0.0
    allocate(  SFIPWAT(1) ); SFIPWAT  =0.0
    allocate(     SGAS(1) ); SGAS     =0.0
    allocate(     SGRP(1) ); SGRP     =0.0
    allocate(    SOMAX(1) ); SOMAX    =0.0
    allocate(     SWAT(1) ); SWAT     =0.0
    allocate(     SWEL(1) ); SWEL     =0.0
    allocate(  WAT_DEN(1) ); WAT_DEN  =0.0
    allocate(    WATKR(1) ); WATKR    =0.0
    allocate( WATQUIES(1) ); WATQUIES =0.0
    allocate( WAT_VISC(1) ); WAT_VISC =0.0
    allocate(     XCON(1) ); XCON     =0.0
    allocate(     XGRP(1) ); XGRP     =0.0
    allocate(     XWEL(1) ); XWEL     =0.0
    allocate(     ZGRP(1) ); ZGRP     ='AAAAAAAAAA'
    allocate(     ZWEL(1) ); ZWEL     ='AAAAAAAAAA'
    allocate(     ZWLS(1) ); ZWLS     ='AAAAAAAAAA'
